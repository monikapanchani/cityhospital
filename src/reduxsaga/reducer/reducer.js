import * as ActionTypes from "../Actiontype";

const initVal = {
  isLoading: false,
  user: null,
  error: "",
};

export const authreducer = (state = initVal , action) => {
  switch (action.type) {
    default:
      return state;
  }
};
